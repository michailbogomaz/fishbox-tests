# Инструкции fast — Тип B (grep-проверки)

1. **Подтверждение инструкций.** Перед стартом выведи блок  
   `Подтверждение инструкций — [./.cursor/checklist-instrusctions_fast_type-b.md#L1](./.cursor/checklist-instrusctions_fast_type-b.md#L1)`  
   и явно укажи, что файл прочитан полностью. Делай это только после полного завершения типа A.
2. **Язык.** Отвечай пользователю только на русском.
3. **Подготовка (используй результаты шага A):**
   - список файлов и порядок из `git log` уже собран — переиспользуй его;
   - повторно определи интерфейс (REST/GraphQL) только если информация изменилась;
   - загрузить чеклист `./.cursor/checklist-type-b.md`.
4. **Общие правила:**
   - для каждого паттерна запускай ровно одну команду `rg`/`grep`/`python` по списку изменённых файлов;
   - фиксируй пустой вывод так же, как и любое найденное совпадение;
   - графQL-специфичные пункты (например, 39) помечай ⏭️, если сервис — REST.
5. **Формат блока:**
   ```
   Проверяю пункт N (тип B) — [./.cursor/checklist-type-b.md#L<номер>](./.cursor/checklist-type-b.md#L<номер>)
   Запускаю команду `<command>`
   Результат: …
   Статус: ✅/❌/⏭️ (причина)
   ```
6. **Команды:** перед каждым `run_terminal_cmd` указывай точный список файлов (например, через аргументы `rg file1 file2`). Если используешь `python`, перечисли пути внутри скрипта.
7. **Порядок:** выполняй пункты в том же порядке, что и в чеклисте; не смешивай результаты нескольких пунктов.
8. **Финал по типу B:** перечисли статусы всех пунктов типа B и перечисли найденные нарушения/рекомендации.

> Закончив тип B, переходи к инструкциям типа C (если он указан в параметре `type`).
